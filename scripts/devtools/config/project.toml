# This checked-in file deliberately contains no secrets. Adapt it for this project.

[atlassian]
# Either a Cloud-ID UUID or canonical site URL, e.g. https://example.atlassian.net.
cloud_id = "https://manmik.atlassian.net"

[jira.presets.bug]
project = "JUSTDEV"
issue_type = "Bug"
labels = ["auto-filed"]

[bitbucket]
workspace = "REPLACE"
repository = "REPLACE"
username = "REPLACE"
target_branch = "main"
reviewers = []

[jenkins]
url = "https://jenkins.example.invalid"
username = "REPLACE"

[jenkins.presets.test]
job = "REPLACE/job/test"
allowed_parameters = ["REF"]

[confluence]

[confluence.presets.release-notes]
page_id = "REPLACE"
title = "Release notes"

[project]
# Direct-CLI fallback only. Customize project_verify_command in recipes/project.just.
starter_hook = true
verify_commands = []
